<UserControl x:Class="DmxPad.DmxView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dm="clr-namespace:Datamodel;assembly=datamodel.net" xmlns:l="clr-namespace:DmxPad" xmlns:c="clr-namespace:DmxPad.Controls"
             d:DataContext="{Binding Source={d:DesignInstance Type=l:DesignTimeData,IsDesignTimeCreatable=True},Path=[0]}"
             mc:Ignorable="d" d:DesignHeight="397" d:DesignWidth="664">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Name="ValueColumn" Width="{Binding SelectedItem, Converter={StaticResource ValueColumnWidthConverter}, ElementName=DmxTree, Mode=OneWay}"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="2" VerticalAlignment="Top" >
            <TextBox Name="PathBox" Margin="0,4" Padding="18,1,0,1" Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=SelectedPath,Mode=OneWay,StringFormat='//{0}'}" BorderBrush="LightGray" />
            <Image Source="/DmxPad;component/Resources/datamodel.png" SnapsToDevicePixels="True" HorizontalAlignment="Left" Height="16" Width="16" Margin="4,0" Cursor="Hand" 
                   MouseDown="ResetRoot_Click" ToolTip="Return to the root element"/>
        </Grid>

        <c:TreeGridView x:Name="DmxTree" Grid.Row="1" Grid.Column="0" ItemsSource="{Binding}" ChildItemsSelector="{Binding Converter={StaticResource ChildPathConverter}}"
                        UseLayoutRounding="True" d:DataContext="{Binding Path=AllElements}" TitleColumnHeader="Name">
            <c:TreeGridView.ColumnDefinitions>
                <c:TreeGridContentColumnDefinition Header="Type" Binding="{Binding Converter={StaticResource GetFriendlyTypeName}}">
                    <c:TreeGridContentColumnDefinition.CellStyle>
                        <Style TargetType="c:TreeGridViewCell">
                            <Setter Property="Foreground" Value="Gray"/>
                        </Style>
                    </c:TreeGridContentColumnDefinition.CellStyle>
                </c:TreeGridContentColumnDefinition>
                <c:TreeGridColumnDefinition Header="Value" ItemTemplateSelector="{StaticResource ValueColumnTemplateSelector}"/>
            </c:TreeGridView.ColumnDefinitions>
            <c:TreeGridView.TitleTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}"/>
                </DataTemplate>
            </c:TreeGridView.TitleTemplate>
            
            <c:TreeGridView.Resources>
                <DataTemplate DataType="{x:Type dm:Attribute}">
                    <StackPanel Orientation="Horizontal" Margin="0,0,5,0" VerticalAlignment="Center">
                        <ContentControl Content="{Binding Converter={StaticResource AttributeIconConverter}}" IsTabStop="False" />
                        <TextBlock Text="{Binding Path=Name}" Padding="5,0,0,0" />
                    </StackPanel>
                </DataTemplate>

                <DataTemplate DataType="{x:Type dm:Element}">
                    <StackPanel Orientation="Horizontal" Margin="0,0,5,0" VerticalAlignment="Center" AllowDrop="True" Drop="ElementHeader_Drop" MouseDown="ElementHeader_MouseDown" MouseMove="ElementHeader_StartDrag" KeyDown="ElementHeader_KeyDown">
                        <ContentControl Content="{Binding Converter={StaticResource AttributeIconConverter}}" IsTabStop="False" />
                        <TextBlock Text="{Binding Path=Name}" Padding="5,0,0,0" FontStyle="Italic" />
                    </StackPanel>
                </DataTemplate>

                <Style TargetType="c:TreeGridViewItem">
                    <EventSetter Event="Selected" Handler="TreeViewItem_Selected"/>
                </Style>
            </c:TreeGridView.Resources>
        </c:TreeGridView>

        <GridSplitter Grid.Column="1" Grid.Row="1" Width="2" VerticalAlignment="Stretch" HorizontalAlignment="Left" ResizeDirection="Columns"/>
        
        <Grid Name="AttributeProperties" Grid.Column="1" Grid.Row="1" Margin="5,0" Background="{DynamicResource {x:Static SystemColors.WindowColor}}"
                  d:DataContext="{Binding Path=Root[model]}" DataContext="{Binding ElementName=DmxTree, Path=SelectedItem}" Visibility="{Binding ElementName=ValueColumn,Path=Width,Converter={StaticResource ValuePanelVisibilityConverter}}">
            <ContentControl Content="{Binding Converter={StaticResource GetAttributeValue}}" ContentTemplateSelector="{StaticResource InspectPaneTemplateSelector}" />
        </Grid>
    </Grid>
</UserControl>
